import java.util.ArrayList;

public class test {

	public static void main(String[] args){
	
	Library 
		
	
	//String libraryNameToImport = importTextField.getText().trim();
	String libraryNameToImport = "booklist.csv";
	//takes the variable name of the csv file and loads it from the library.
	library.loadLibraryFromCSV(libraryNameToImport);
	ArrayList<Book> libraryCopy = library.getBooks();
	int numItems = libraryCopy.size();
	//create conditional checks to make sure csv is valid, if valid:
	
	}
}
